fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
litellm>=1.44
langgraph>=0.2
langchain-core>=0.3
asyncpg>=0.29
aio-pika>=9.4
sentence-transformers>=3.0
httpx>=0.27
onnxruntime>=1.18          # inference CPU du modèle de triage (S3-J3)
sentencepiece>=0.2         # tokenizer XLM-R
keybert>=0.8               # extraction de mots-clés (S3-J4)
langfuse>=2.0              # observabilité des appels LLM (S3-J5)
pymupdf>=1.24              # extraction PDF dans l'ordre de lecture (base de connaissances, S5-J1)
rank-bm25>=0.2.2           # recherche lexicale BM25 pour le retrieval hybride (S5-J2)
python-multipart>=0.0.9    # upload multipart des documents KB (FastAPI UploadFile)
sqlglot>=25.0              # analyse syntaxique du SQL genere par l'agent Insight (S6-J1)
markdown>=3.6              # Markdown -> HTML pour le rendu PDF du digest (S6-J4), pur Python
weasyprint>=62             # HTML -> PDF du digest ; depend de pango/cairo (voir Dockerfile)
umap-learn>=0.5            # reduction de dimension avant le clustering des sujets (S7-J1)
statsmodels>=0.14          # decomposition STL des series horaires (anomalies de volume, S7-J2)
lightgbm>=4.3              # modele de risque de depassement SLA (S7-J3) ; repli sur regle si absent
python-docx>=1.1           # ingestion documentaire : lecture des .docx (S7-J4)
pytesseract>=0.3           # OCR **en repli** sur les PDF scannes (S7-J4) ; exige le binaire tesseract
pillow>=10.0               # rendu des pages PDF en image avant OCR
# HDBSCAN vient de scikit-learn (>= 1.3), deja tire par sentence-transformers : pas de dependance
# supplementaire ni de compilation, contrairement au paquet `hdbscan` autonome.
# Ajoutés au fil des semaines : keybert, rank-bm25, sqlglot, lightgbm, bertopic, langfuse,
# pymupdf, python-docx, pytesseract (ingestion documentaire S7)
