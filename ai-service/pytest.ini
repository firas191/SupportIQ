[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: exercice une chaine lourde (compilation JIT d'UMAP). Execute par defaut : une garantie
        qu'on ne lance pas n'en est pas une. Utiliser `-m "not slow"` pour une boucle rapide.
